<?xml version="1.0" encoding="utf-8" ?>
<Page:PopupPage
    xmlns:Page="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup" 
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="CookItApp.Views.PopupRestablecerContrasenaPage"
    Title="Restablecer contraseña">
    
    <StackLayout Margin="12"
                 Padding="24"
                 BackgroundColor="White"
                 HorizontalOptions="Center"
                 VerticalOptions="Center">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50*"/>
                <ColumnDefinition Width="50*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <Label Grid.Column="0" Grid.Row="0" Text="Ingrese su email:" TextColor="Black" FontSize="16" />

            <Entry Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" x:Name="entryEmail" Placeholder="Email" FontSize="16"/>

            <Button x:Name="BtnRestablecer" Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2" FontSize="16" Text="Restablecer"  TextColor="White" BackgroundColor="#00748A" Clicked="BtnRestablecer_Clicked"/>

        </Grid>

    </StackLayout>
</Page:PopupPage>